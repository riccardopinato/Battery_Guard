name: battery_guard
description: Battery Guard - monitoraggio intelligente della batteria e avvisi di ricarica.
publish_to: "none"
version: 0.4.0+5

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
